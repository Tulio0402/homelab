# 工作日誌

---

## 2026-08-15（五）

**做了**
- 安裝 WSL2 + Ubuntu 24.04
- 於 `/etc/wsl.conf` 啟用 systemd，`wsl --shutdown` 後生效
- 產生 ed25519 SSH 金鑰並加到 GitHub，`ssh -T git@github.com` 驗證通過
- 建立 `~/homelab` 目錄結構，初始化 git 並推上 GitHub



